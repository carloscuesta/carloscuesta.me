<div class="wrap">
	{{#foreach posts}}
		<article class="col-xs-12 {{post_class}} post">
		    <header class="row">
		    	<div class="col-xs-12">
		        	<h2 class="post-title preview">
		        		<a href="{{url}}">{{{title}}}</a>
		        	</h2>
		        </div>
		        <div class="col-xs-12 post-info">
		        	<time class="post-date" datetime="{{date format='YYYY-MM-DD'}}">{{date format="DD MMMM YYYY"}}</time>
		        	<span class="tags">
		        		{{#foreach tags}}
        					<a href="{{url}}" title="{{name}}" class="tag-{{name}}">{{name}}</a>
    					{{/foreach}}
    				</span>
		        	<span class="author">{{author}}</span>
		        </div>
		    </header>
		    <section class="post-excerpt">
		        <p>{{excerpt words="26"}} ...
		    	<a class="read-more" href="{{url}}"> Read more &raquo;</a></p>
		    </section>
		</article>
	{{/foreach}}
	<nav class="pagination col-xs-12 center-xs" role="navigation">
		{{pagination}}
	</nav>
</div>
