section.github
    .wrap
        h2 GitHub
        .row.around-xs
            -each github in githubData.items
                article.github-repo.col-xs-12.col-sm-6.col-md-4
                    div(class="repo language-#{github.language}")
                        a(href="#{github.html_url}", onmousedown="ga('send', 'event', 'GitHub', '#{github.name}');", target="_blank") 
                            h4.github-title #{github.name}
                        .github-stats
                            a.starred(href=github.html_url)
                                svg.icon.icon-star
                                    use(xlink:href="#icon-star")
                            span.stars #{github.stargazers_count}
                            if github.forks
                                a.forked(href="#{github.html_url}/fork")
                                    svg.icon.icon-fork
                                        use(xlink:href="#icon-fork")
                                    span.forks #{github.forks}
                        .description #{github.description}
            a.btn.btn-large.btn-blue(href="https://github.com/#{social.github}", onmousedown="ga('send', 'event', 'Social', 'Follow GitHub');", target="_blank")
                svg.icon.icon-github
                        use(xlink:href="#icon-github")
                | Follow @#{social.github}
